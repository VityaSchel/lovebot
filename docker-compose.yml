version: "3.9"

services:
  frontend_panel:
    image: registry.gitlab.com/prank_bot/prank_landing_new:main
    restart: unless-stopped
    expose:
      - "80"
    environment:
      - VIRTUAL_HOST=xprank.xyz
      - VIRTUAL_PORT=80

      - PORT=80

networks:
  default:
    external:
      name: nginx-proxy
